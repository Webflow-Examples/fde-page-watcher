# R1 — review pass over the F2 / F3 / colour-migration merge

**Branch:** `chunk-r1` · **Base:** `0e9ca99` · **Scope:** review only. No UI, no
new product behaviour.

Every finding below carries exactly one disposition: **fixed here**, **handed to
a named chunk**, or **accepted with a reason**.

---

## 00 · What this pass could not start from

The brief says to start from section 05 of the chunk doc and extend it. That
document is not in the repository — `docs/`, `audits/` and the git history hold
no file with numbered sections 04 and 05, and nothing in `vocabulary.json` or
`DECISIONS.md` carries them.

So the inventory in section 02 was rebuilt from the code rather than extended
from the existing one. **The verify item "the pair inventory is longer than the
one you started from" is therefore unverified** — I cannot compare against a
list I do not have. If the original section 05 is to hand, the two should be
diffed; any row it holds that section 02 does not is a row this pass missed.

The five defect classes of section 04 are likewise not quoted. The classes
actually hunted are named in section 03, so the gap is visible rather than
implied.

## 01 · The state at the start of the pass

`npm run lint`, `npm run typecheck` and `npm test` were all green on `0e9ca99`
before any change. In particular:

- **The 4 `scoring.test.ts` colour failures do not reproduce.** 35 tests, all
  passing. The colour assertions in that file are the "returns no colour of any
  kind" family and they pass.
- **The 5 Sparkline typecheck errors do not reproduce.** `Sparkline` no longer
  takes a `color` prop at all — it takes `device` and resolves the hue itself.
  The prop the errors were about is gone.

Both look to have been cleared by the work already merged. Nothing was skipped,
`.skip`/`.todo`/`@ts-expect-error`/`eslint-disable` appear nowhere in `src/`, so
the green is real rather than suppressed. **Disposition: accepted — no longer
present.** If the failures were observed on a branch that is not `main`, they
need re-reporting against a commit.

## 02 · The pair inventory

A pair is one fact stated in two places. The **Bound by** column is what stops
them disagreeing — the point of the exercise is to move rows from *comment* and
*nothing* up to *derivation*.

### 2.1 Registry ↔ mirroring constant

| # | The fact | Stated in | Bound by (before) | Disposition |
|---|---|---|---|---|
| P1 | work states, labels, tones, meanings | `vocabulary.json` ↔ `vocabulary.ts` | parity test | unchanged — already derived |
| P2 | queue membership (`holds`) | `vocabulary.json` ↔ `QUEUE_HOLDS` | parity test | unchanged |
| P3 | **which queues carry a badge** | `queue.counted: false` ↔ `COUNTED_QUEUES` | **nothing** | **fixed** — parity test added |
| P4 | **who fires each transition** | `action.actor` ↔ `IssueTransition.actor` | **`resolve` only** | **fixed** — asserted for all six |
| P5 | **what each transition requires** | `action.requires` ↔ `IssueTransition.requires` | **`resolve` only, and only in one test** | **fixed** — asserted for all six |
| P6 | transition from/to | `vocabulary.json` ↔ `ISSUE_TRANSITIONS` | parity test | unchanged |
| P7 | checkpoint results and evaluation prose | `vocabulary.json` ↔ `CHECKPOINT_*` | parity test | unchanged |
| P8 | actionability + reason requirement | `vocabulary.json` ↔ `ACTIONABILITY_*` | parity test | unchanged |
| P9 | banned terms | `banned_global.terms` ↔ eslint rule | rule reads the JSON | unchanged — already derived |
| P10 | allowlisted files | `banned_global.allowlist` ↔ eslint config | config reads the JSON | unchanged — already derived |
| P11 | allowlist baseline | registry allowlist ↔ `ALLOWLIST_BASELINE` | ratchet test | unchanged — the baseline is *meant* to be a frozen copy |
| P12 | **the confidence union** | `confidence` concept ↔ inline `"confirmed" \| "probable" \| "unclear"` on `IssueCase` | **nothing** | **fixed** — imports `Confidence` |
| P13 | **every evidence slot has a producer (rule 15)** | registry ↔ the two producer maps | **prose in three comments, plus one assertion that `is-agentic` had gone** | **fixed** — the union of the producer maps is now asserted to equal `EVIDENCE_SOURCES` |

### 2.2 State machine ↔ migration adapter

| # | The fact | Stated in | Bound by (before) | Disposition |
|---|---|---|---|---|
| P14 | **legacy `done` → which work state** | `stateFromTaskStatus` (`issue-case.ts`) ↔ `TASK_STATUS_WORK_STATE` (`workState.ts`) | **a comment saying "must stay in step"** | **fixed** — asserted against each other. *This is the pair the known bug lived in.* |
| P15 | legacy `in-progress` → which work state | same two files | comment | **fixed** — same test covers it |
| P16 | legacy `todo` → which work state | same two files | comment | accepted — the two **differ on purpose** and the test says so explicitly |
| P17 | provider result union | `AgentIssueCheckResult` ↔ `ExternalAgentCheckResult` | bidirectional assignability test | unchanged — already derived, and the pattern the rest of this pass copies |
| P18 | **merge precedence covers every state** | `MERGE_PRECEDENCE` ↔ `WORK_STATES` | **nothing** (`indexOf` → −1) | **fixed** — `Record<IssueState, number>`, so a new state is a compile error |
| P19 | **confidence precedence covers every value** | `CONFIDENCE_PRECEDENCE` ↔ `CONFIDENCES` | **nothing** (`indexOf` → −1) | **fixed** — same |
| P20 | effort ordering | `EFFORT_ORDER` (`issue-case.ts`) ↔ `EFFORT_ORDER` (`store.tsx`) | nothing | **accepted, with a note** — see §04 F7 |

### 2.3 Token role ↔ token usage

| # | The fact | Stated in | Bound by (before) | Disposition |
|---|---|---|---|---|
| P21 | which tokens are painted as ink | source ↔ contrast coverage | discovery from source | unchanged — already derived, and the model for this section |
| P22 | **the tone list the chip can ask for** | `vocabulary.ts` `TONES` ↔ a hand copy in `token-contrast.test.ts` | **nothing — a mirror asserted against a mirror** | **fixed** — the test imports the registry's tones |
| P23 | **the health bands the token block defines** | `globals.css` ↔ a hand list in the test | **nothing** | **fixed** — discovered from the stylesheet |
| P24 | **trend glyphs** | `TREND_ARROW` (`scoring.ts`) ↔ `TREND_GLYPH` (`trend-arrow.tsx`) | **a comment** | **fixed** — one map; the component reads it |
| P25 | **score band token names** | `SCORE_META` ↔ `SCORE_META_VARS` | **nothing — two parallel literals** | **fixed** — the wrapped map is derived from the named one |
| P26 | **device identity hue** | `seriesToken` (`charts.tsx`) ↔ `seriesVar` (`pages/[id]`) ↔ `SERIES_VAR` (`ScoreCard.tsx`) | **nothing — three literal copies** | **fixed** — one map; a fourth copy now fails a test |
| P27 | manifest colours ↔ `:root` | `manifest.ts` ↔ `globals.css` | test asserts the values match | unchanged — derived; only the comment ("by hand") is stale |
| P28 | **theme resolution** | `resolveSurface` ↔ `APPEARANCE_PREPAINT_SCRIPT` | **nothing — no test existed at all** | **fixed** — the real script is executed and compared, both directions |
| P29 | the `data-surface` values | pre-paint script ↔ `globals.css` block selector | nothing | **fixed** — same new test |

### 2.4 Rows left open

| # | The fact | Disposition |
|---|---|---|
| P30 | `--confidence-weak` vs `--confidence-strong` as a strength axis ↔ the Issues list painting every confidence weak | **handed to W1** (§04 F5) |
| P31 | the registry's `confidence` (`confirmed/probable/unclear`) ↔ native-element `confidence` (`high/medium/low`) | **handed to S3** (§04 F6) |
| P32 | `charting.ts` defaults ↔ "a caller relying on the defaults must stay in step" | not investigated — **left open**, named here so it is not lost |
| P33 | `agentAuditServer.ts` ↔ `webflowConnectionServer.ts` ("mirrors, including the no-collector development path") | not investigated — **left open** |
| P34 | `scoreCard.ts` `trustedHistorySegments` mirror (two comments claim a mirror) | not investigated — **left open** |

The inventory is not finished. P32–P34 are the rows this session did not reach.

## 03 · The defect classes actually hunted

Section 04 of the chunk doc was unavailable, so these are the classes searched
for, stated so the gap is auditable:

1. **Two statements of one fact that disagree** — §02, and F1/F2 below.
2. **A test that asserts the implementation rather than the decision** — P14,
   P22, and the two generic transition tests rewritten in §04 F2.
3. **A silent fallback** — `?? 0`, `?? ""`, `?? 100`, `indexOf` → −1. F3, F4,
   P18, P19.
4. **A requirement stated and not enforced** — F2.
5. **A slot, token, or branch with no producer** — P13, F5, F8.

## 04 · Findings

### F1 — an unavailable reading counted as a system that disagrees · **fixed**

`fromAgentIssue` filtered `ignored` and `not-applicable` out of the evidence
ledger but left `unavailable` in. `EvidenceEntry.supports` is a boolean, so an
unavailable source landed as `supports: false` — indistinguishable from a system
saying *the problem is not there*. `confidenceFrom` returns `unclear` on any
dissent, so **one provider outage silently downgraded a corroborated diagnosis
to Unclear**, and the ledger showed a reading that had never been taken.

Three statements in the codebase already said this was wrong:

- `agentIssueCases.ts` header, rule 2: "Ignored, not-applicable, unavailable,
  partial, and failed are five distinct outcomes and never collapse into one
  another." Its own `caseStatus` and `caseConfidence` keep `unavailable` out of
  the determined set. The adapter downstream did not.
- Registry `checkpoint.unavailable`: "Neither agreement nor disagreement. Same
  condition as `agent_result.unavailable` on a different object."
- Registry rule 18: an absent measurement is not a small measurement.

**Fix:** `unavailable` joins the unreadable set. **Tests:** two, asserting
against the registry's sentences rather than the assembler's code. Verified by
reverting the fix — both fail; restored — both pass.

### F2 — `resolve` states a requirement nothing enforced · **fixed**

The registry says `action.resolve.requires: "checkpoint_agreement"`, and
`ISSUE_TRANSITIONS.resolve.requires` carried the word. `applyAction` only ever
enforced `requiresReason`. A case could reach **Resolved with three checkpoints
still scheduled** — the state whose whole meaning is "the evidence agreed and
held".

Nothing calls it today, so this never shipped a wrong Resolved. It is a
contradiction between two statements in the code, not a live incident.

**Fix:** the guard is enforced from the table (`transition.requires`), not from
a branch on the action's name, so a requirement added to `vocabulary.json`
cannot be carried in the type and ignored in the guard again. `checkpointsAgree`
is the registry's evaluation rules 1, 3, 4 and 5, and the interval that can fire
it is read off `CHECKPOINT_DAYS` rather than written as `"30d"`.

Two existing tests — "throws on every transition the registry does not name" and
"lands on the state the registry names" — were **asserting a legal move without
its stated requirement**. They now build the case from `action.requires`, the
same way they already built the reason from it. A requirement added to the
registry arrives there as a missing arm rather than a silently-satisfied one.

**Tests:** seven, one per registry evaluation sentence. Verified by mutation.

**Note for the owning chunk:** the checkpoint *scheduler* is still unbuilt.
`mark_fixed` schedules three checkpoints, nothing ever evaluates them, so
`resolve` remains unreachable in practice and `Watch` never drains. That is a
feature, not a review fix — **handed to the chunk that builds the case view**
(S2 on the current map; correct the owner if the chunk doc says otherwise).

### F3 — the Watcher's "stable across the board" claim · **accepted, with a reason**

`watcher.ts` builds its Accessibility/SEO stability claim like this:

```
(pageRangeComparison(p, strategy, key, rangeDays)?.delta ?? 0) <= -regression
&& (pageRangeComparison(p, strategy, key, rangeDays)?.to ?? 100) < regressionFloor
```

`rangeComparison` returns `null` when a page has fewer than two nights in range.
`?? 100` **fabricates a perfect score for a page that was never measured**, and
`?? 0` fabricates "no movement". If either were reached, an unmeasured page
would count as not-dropped and the Watcher would print *"Accessibility and SEO
are stable across the board."* — the exact unchecked claim the comment two lines
above forbids.

**It is not currently reachable.** All four categories live on the same `Night`
objects, so if `perf` has a comparison every category does; and a page without
one is `pending`, which `ranked` excludes. Probed directly to confirm: a
one-night page yields `comparison = null`, `trend = "pending"`, `winning = null`.

**Accepted rather than fixed**, because a fix here would be behaviour-identical
and could carry no failing test — which makes it a refactor, not a review
change. The reason it is safe is now written down: **it depends on `NightScores`
carrying all four categories on every night.** If that ever becomes partial,
this reopens as a live defect.

### F4 — `confirmedRuns: options.confirmedRuns ?? 0` · **handed to S2**

`fromRec` sets `confirmedRuns` to 0 for every migrated case, because the legacy
record carries no count. The registry says `new` means "Detected **and
corroborated**". So a migrated case claims corroboration while recording zero
corroborating runs — rule 18's `?? 0`-on-an-unmeasured-value shape.

Harmless today: `confirmedRuns` is written in three places and **rendered
nowhere**. Handed to the chunk that first renders it, which must distinguish
"not counted" from "counted zero" rather than printing a 0.

### F5 — the Issues list paints every confidence weak · **handed to W1**

`issue-row.tsx:174` and `issue-group.tsx:81` both render
`CONFIDENCE_LABEL[...]` in `var(--confidence-weak)` unconditionally. The token
layer defines strength as a two-token axis and `bits.tsx` uses it that way, so
the word reads **Confirmed** while the colour says *weak*. Registry rule 13: a
token is named for its role.

Not fixed here — whether the Issues list wants strength encoded as hue is a
design decision, and R1 is explicitly no-UI. Handed to W1.

### F6 — two confidence vocabularies · **handed to S3**

`confidenceVar` in `pages/[id]/page.tsx` branches on `"high"`/`"medium"`, which
is `NativeElementFinding.confidence` — a second confidence scale alongside the
registry's `confirmed / probable / unclear`, rendered on the same screen.
Registry rule 4: no word carries two different meanings.

S3 already owns this: the registry's `dismissed.absorbs` note assigns
native-element findings to S3, and `pages/[id]/page.tsx` is on the banned-
vocabulary allowlist against S3.

### F7 — two constants named `EFFORT_ORDER`, meaning different things · **fixed, and flagged**

`issue-case.ts` had `EFFORT_ORDER` as an array ranking bands for *merging*
(`unknown` loses to any known band). `store.tsx` has `EFFORT_ORDER` as a
`Record<Effort, number>` ranking bands for *sorting* (`unknown` sorts **last**,
value 3). Same name, same concept, same type shape after this pass — opposite
placement of `unknown`. Unifying them would silently break one of the two.

The merge-side constant is now `EFFORT_WIDTH: Record<Effort, number>`, exhaustive
by type, with a comment saying explicitly that it must not be unified with the
other one.

> **This is the one rename in the diff, and the brief said not to rename.** The
> shape and meaning changed as part of removing the `indexOf` → −1 fallback, and
> leaving it called `EFFORT_ORDER` would have preserved a name collision between
> two different orderings. Reverse it if you would rather hold the line.

### F8 — the ink-token discovery silently drops undefined tokens · **accepted, with a reason**

`discoverInkTokens` ends `.filter((t) => !INK_EXEMPT.has(t) && t in LIGHT)`. A
mistyped token name is not in `LIGHT`, so it is dropped from contrast coverage
rather than reported — a token that renders with no colour at all would be
invisible to the test whose entire purpose is catching that.

Checked: **zero tokens are currently dropped by that filter.** The
locally-scoped pass-throughs the comment mentions (`--segment-tone`) are not
matched by the `color:` pattern in the first place, so the filter is dormant.

Accepted rather than fixed: turning it into an assertion is a real improvement
but not a contradiction, and this pass has no evidence of a live typo. Recorded
so the next pass has a reason to revisit it.

### F9 — a stale comment naming a prop that no longer exists · **fixed in passing**

`pages/[id]/page.tsx` carried a type comment justifying itself by "when
`charts.tsx` narrows `Sparkline.color` off `string`". `Sparkline` takes `device`
and has no `color` prop. The comment went with the code it justified when P26
was derived.

## 05 · Proposed registry rules

Both come from the shape of the failures above, not from any one of them.

**Rule 20 — a fact stated twice is a defect waiting, so state it once.**
Where a second statement is genuinely unavoidable (a pre-paint script that
cannot import; a type that must not reach a provider module), the second copy
carries a test that *executes* or *type-checks* the two against each other. A
comment asking the next editor to remember is not a mechanism. Every failure in
§04 that had a "keep these in step" comment had a comment and nothing else.

**Rule 21 — a test asserts the decision, not the code.**
`expect(migrated.state).toBe("resolved")` passes whatever the registry says.
`expect(migrated.state).toBe(taskStatusWorkState("done"))` fails the moment the
two halves of the decision disagree. Where a test names a literal that
`vocabulary.json` also names, it is asserting a mirror against a mirror and
proves only that two copies agree — never that either is right.

## 06 · Verification

- `npm run lint` — clean, no exceptions.
- `npm run typecheck` — clean, no exceptions.
- `npm test` — 85 files, 959 tests, all passing (up from 84 / 930).

Every converted pair was verified by mutation — source changed, test observed to
fail, source restored, test observed to pass:

| Pair | Mutation | Result |
|---|---|---|
| P14 | `TASK_STATUS_WORK_STATE.done` → `"fixed"` *(the original bug)* | 2 tests fail |
| P13 | added `is-agentic` back to `EVIDENCE_SOURCES` | 2 tests fail |
| P22 | added a sixth tone to `TONES` | 4 tests fail |
| P18/P19 | added an eighth work state | typecheck fails in 2 places in `issue-case.ts` |
| P26 | re-added a literal `--series-desktop` to `ScoreCard.tsx` | 1 test fails |
| P28 | dropped `"light"` from the pre-paint guard | 1 test fails |
| P28 | flipped Auto in `resolveSurface` | 2 tests fail |
| F1 | removed `unavailable` from the unreadable set | 2 tests fail |
| F2 | removed the checkpoint guard | 4 tests fail |

`ALLOWLIST_BASELINE` was not widened. No guard was relaxed to make a check pass.

## 07 · What is still open

- The pair inventory is unfinished: **P32, P33, P34**.
- Section 05 of the chunk doc was never compared against §02 (see §00).
- The checkpoint scheduler is unbuilt, so `Watch` never drains — F2's note.
- F5 (W1), F6 (S3), F4 (S2) are handed over, not done.
