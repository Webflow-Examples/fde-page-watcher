// This View performs no host capability calls — it only displays a
// locally-derived URL for the user to open manually in the Site Browser.
// The intent map is kept empty on purpose.
export type Intents = Record<string, never>;
